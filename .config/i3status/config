general {
  colors = true
  interval = 1
}

order += "wireless wlp3s0"
order += "disk /"
order += "battery 0"
order += "battery 1"
order += "tztime local"

wireless wlp3s0 {
  format_up = " %essid %quality "
  format_down = " "
}

disk "/" {
  format = " %avail "
  prefix_type = custom
}

battery 0 {
  format = " %status %percentage %remaining "
}

battery 1 {
  format = " %status %percentage %remaining "
}

tztime local {
  format = " %Y-%m-%d %H:%M "
}
