#!/bin/sh
#
# Focus a given window or exec the given arguments

class=$1
shift

n=$(ratpoison -c 'windows %n %c' | awk '/^[0-9]+ '$class'$/ { print $1 }')

if [ -n "$n" ]; then
  ratpoison -c "select $n"
else
  exec $@
fi
