settings = {
  core = {
    real_name = "Eivind Uggedal";
    user_name = "uggedal";
    nick = "uggedal";
  };
  "fe-text" = {
    actlist_sort = "refnum";
    scrollback_lines = "50000";
  };
};

chatnets = {
  freenode = {
    type = "IRC";
  };
};

servers = (
  {
    address = "holmes.freenode.net";
    chatnet = "freenode";
    port = "7000";
    use_ssl = "yes";
    ssl_verify = "yes";
    autoconnect = "yes";
  }
);

channels = (
  {
    name = "#alpine-linux";
    chatnet = "freenode";
    autojoin = "yes";
  },
  {
    name = "#alpine-devel";
    chatnet = "freenode";
    autojoin = "yes";
  }
);

ignores = (
  {
    level = "JOINS PARTS QUITS";
  }
);

aliases = {
  J = "join";
  N = "names";
  M = "msg";
  T = "topic";
  C = "clear";
  WC = "window close";
};

statusbar = {
  default = {
    window = {
      disabled = "yes";
    };
  };
};
